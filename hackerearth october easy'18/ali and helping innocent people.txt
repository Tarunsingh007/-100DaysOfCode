#include <iostream>
#include <string>
#include <sstream>
#include <vector>
#include <iomanip> 
using namespace std;
 
int main()
{
   string s,str;
   cin>>s;bool c=true,t=true;
   vector<int> p;
   for(int i=0; i<s.length(); i++)
   {
       if(s[i]=='A'||s[i]=='E'||s[i]=='I'||s[i]=='O'||s[i]=='U'||s[i]=='Y')
       {
           c=false;
       }
       if(s[i]>=47 && s[i]<=57)
       str=str+s[i];
   }
          stringstream ss;
          ss<<str;
          int n;
          ss>>n;
   for(int i=n; i!=0; i=i/10)
   {
        int r=i%10;
        p.push_back(r);
   }
   for(int i=1; i<=p.size(); i++)
   {
     if((p[i]+p[i-1])%2!=0)
     t=false;
   }
   if(c&&t)
   cout<<"valid";
   else
   cout<<"invalid";
   return 0;
}